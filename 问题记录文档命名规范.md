<!--
name: 问题记录文档命名规范
creator: Li Cheng
created: 2026-07-01
modified: 2026-08-21
-->

# 问题记录文档命名规范

> 适用范围：`work_space` 工作区下记录**问题**的文档。
> 一篇问题记录 = 一个问题，记录其时间、背景、现象、分析与修复全过程。
> 命名格式**参照开发文档**，以日期开头，并与开发文档相互对应。

---

## 1. 目录结构与文件命名

### 1.1 目录结构（年/月两级）

问题文档按**发现日期**归入 `<年>/<年-月>/` 两级目录，配套三类 `00-README.md` 索引：

```
problems/
├── 00-README.md              ← 顶层·按【类别】索引(跨年月聚合，只放链接+状态)
└── 2026/
    ├── 2026-07/
    │   ├── 00-README.md       ← 当月·按【时间】列全部问题(表格：序号/问题/状态/类别/一句话)
    │   └── 20260720-01-VSCode-Remote-SSH跳板机认证失败.md
    └── 2026-08/
        ├── 00-README.md
        ├── 20260818-01-H2D-D2H带宽异常低.md
        └── 20260818-02-NCCL通信随训练变慢.md
```

- 月目录名用 `2026-08`（带年前缀，跨年不撞、全局可搜）。
- 文件名保持 `YYYYMMDD-NN-主题.md` 不变（日期自带，move 进月目录零信息损失）。
- 三个 README 统一叫 `00-README.md`（`00-` 前缀排最前，与工作区其它索引一致）。

### 1.2 两个 README 的分工（正交：类别视图 vs 时间线）

| | 顶层 `00-README.md` | 月度 `00-README.md` |
|---|---|---|
| 轴 | **按类别**聚合，跨年月 | **按时间**顺序，限当月 |
| 内容 | 每类下列该类所有问题的「链接 + 状态」 | 表格：序号 / 问题 / 状态 / 类别 / 一句话 hook |
| hook | 不写（避免两处重复） | **单一来源在此** |
| 维护 | 新增问题在对应类别加一行 | 新增问题在当月表加一行 |

### 1.3 受控类别集（`category` 字段取值）

顶层按类别切片，靠元信息头 `category:` 归类；从下表取值，不够再扩表：

| 类别 | 覆盖 |
|---|---|
| `GPU·带宽/通信` | PCIe H2D/D2H、NVLink/NCCL |
| `集群·调度编排` | Volcano/RestartPod/PodGroup/K8s |
| `网络·连接` | SSH/跳板机/IB/RoCE |
| `存储·文件系统` | fs/挂载/配额 |
| `训练·框架运行时` | Megatron/NVRx/torch/dataloader |
| `环境·工具链` | 镜像/依赖/cctl |
| `其他` | 兜底 |

### 1.4 文件命名

统一格式（与开发文档一致）：

```
<日期>-<当日序号>-<问题简述>.md
```

| 部分 | 规则 | 示例 |
|------|------|------|
| 日期 | 八位 `YYYYMMDD`，为问题**发现当天** | `20260701` |
| 当日序号 | 两位数字，从 `01` 起，同一天内递增 | `01` `02` |
| 问题简述 | 简洁中文短语，点出问题，不含空格 | `登录超时问题` `显存泄漏` |
| 扩展名 | 统一 `.md` | |

示例：

```
20260701-01-登录超时问题.md
20260701-02-显存泄漏.md
```

> 问题记录与开发文档共用 `日期-序号` 命名风格，靠**元信息头字段**互相关联（见第 6 节），不靠文件名前缀区分。

---

## 2. 元信息头

每篇文件**开头第一行起**，用 HTML 注释块记录管理信息：

```markdown
<!--
name: <问题简述，与文件名一致>
creator: Li Cheng
created: YYYY-MM-DD
modified: YYYY-MM-DD
category: <受控类别集之一，见 1.3>
fix_commit: <修复该问题的 commit id，未修复留空，见第 6 节>
related_dev: <关联的开发文档名，无则留空或写 -->
```

| 字段 | 含义 | 规则 |
|------|------|------|
| `name` | 问题简述 | 与文件名主题一致 |
| `creator` | 记录人 | 本目录默认 **Li Cheng**，他人记录改为实际姓名 |
| `created` | 发现时间 | 首次记录日期，创建后不再改动 |
| `modified` | 最后修改时间 | 每次更新（分析/修复）后改为当日 |
| `category` | 类别 | 从受控类别集取值（见 1.3）；顶层 `00-README.md` 据此归类 |
| `fix_commit` | 修复 commit | 修复合入后填 commit id（短 hash 即可，可多个）；类比 issue 关联的 PR，见第 6 节 |
| `related_dev` | 关联开发 | 填对应开发文档名（可多个），见第 6 节 |

---

## 3. 问题状态（必填）

采用**最简两阶段**状态机，正文首个标题下用醒目一行标注：

```markdown
**状态**：未修复
```

| 状态 | 含义 |
|------|------|
| 未修复 | 问题已记录，尚未解决（可能仍在排查/修复中） |
| 已修复 | 问题已解决并验证 |

> 状态变更为 `已修复` 时，同步更新 `modified` 并填写修复时间、方案与**修复 commit id**（`fix_commit`，见第 6 节）。

---

## 4. 正文结构（推荐，保持精简）

```markdown
# <日期>-<序号> · <问题简述>

**状态**：未修复

## 1. 背景与环境
时间、代码仓 / 分支 / commit、运行环境（硬件、依赖版本）等定位信息。

## 2. 问题现象
具体表现、报错信息、日志片段（贴关键部分即可）。

## 3. 复现步骤
如何稳定触发；若偶发，说明触发条件与概率。

## 4. 原因分析
排查过程与根因（排查中可持续补充）。

## 5. 解决方案（UT 先行，遵循本规范第 5 节修改范式）
1. 先按根因写/改单元测试（UT），跑通证明其**能复现问题**（改前必红）。
2. UT 确认正确后**冻结 UT**，再动业务代码，直到 UT 由红转绿。
贴关键 UT 代码/路径、修复做法与涉及改动；填写**修复时间**与**修复 commit id**（`git log` 短 hash，可多个）。完成后状态改为「已修复」。

## 6. 关联开发文档
指向引入/修复该问题的开发文档（见本规范第 7 节）。
```

原则：**时间、背景、现象**三项必写清楚，方便日后检索与回溯；分析与方案随进展补充。修改代码一律遵循本规范**第 5 节修改范式**。

---

## 5. 修改范式（UT 先行，先测后改，测冻结）

修复任何问题，**一律先测后改**：先用单元测试把根因钉死，再动业务代码。禁止「先改代码、事后补测」或「无测直接改」。

### 5.1 三步铁律

| 步骤 | 动作 | 通过判据 |
|------|------|----------|
| 1. 设计 UT | 依据「问题现象 + 根因分析」写/改 UT，直指根因，覆盖复现路径与边界 | UT 在**未改代码前跑必须失败（红）**——证明它真的复现了该问题 |
| 2. 确认 UT 正确 | 审阅 UT：断言对准根因、失败原因正是该 bug（非环境/笔误）、覆盖充分 | 确认无误 → **冻结 UT** |
| 3. 改代码 | UT 冻结后才动业务代码，反复跑至 UT 由红转绿 | 全部 UT 转绿且不回改 UT |

### 5.2 冻结 UT（第 2 步后不许动 UT）

- UT 一经确认正确即**冻结**：此后只改业务代码，**不得为迁就实现而修改、放宽或删除 UT**。
- 若改码过程中发现 UT 本身有错（断言写错、根因判断有偏），须**回到第 1 步**：先纠正 UT、重新确认其能复现问题并冻结，再继续改码——而不是在改码途中顺手改 UT 让它变绿。
- 目的：UT 是根因的**独立锚点**；允许边改码边改 UT，等于既当运动员又当裁判，绿灯失去意义。

### 5.3 落到文档

- 「4. 原因分析」定根因后，「5. 解决方案」**首先**贴出 UT（关键代码或文件路径）及其「改前红 / 改后绿」证据，再写代码改动与 `fix_commit`。
- UT 与修复应在**同一次或成对 commit** 落库，便于回溯「哪个测试守着这个 bug」。

---

## 6. 与开发文档 / 修复 commit 的对应

问题记录与开发文档**双向关联**，并在修复后**挂上修复 commit**——整体类比 GitHub 的 **issue ↔ 关联 PR/commit**：

- 问题文档：元信息头 `related_dev` + 第 6 节指向开发文档名；修复后填 `fix_commit` + 在「解决方案」写明 commit id。
- 开发文档：元信息头 `related_issues` 列出本问题文档名；进展记录里记该问题的修复 commit（见 `开发文档命名规范.md` §6）。

示例对应：

```
问题文档  20260701-02-登录超时问题.md
              ├─ related_dev: 20260701-01-登录接口重构.md
              └─ fix_commit: a1b2c3d（修复合入 main 的 commit）
开发文档  20260701-01-登录接口重构.md
              └─ related_issues: 20260701-02-登录超时问题.md
```

> **一问题一修复一 commit（可多）**：像 issue 被某个 PR 关闭一样，问题的修复要能对回具体 commit——
> 便于「这个问题到底哪次改动修的、改了什么」一键回溯。`fix_commit` 是该回溯的锚点。
> 若问题独立于任何开发任务（如线上偶发 bug），`related_dev` 留空；后续若为此专门开展开发，再补建开发文档并回填关联。

---

## 7. 新建问题记录速查

1. 定位目录：`problems/<年>/<年-月>/`（不存在则建，如 `2026/2026-08/`）。
2. 命名：`<发现日期>-<当日序号>-<问题简述>.md`（序号当日从 `01` 递增）。
3. 顶部补元信息头，`creator` 默认 `Li Cheng`，`created`/`modified` 填当日，`category` 从受控类别集取（见 1.3）。
4. 正文首行标 `**状态**：未修复`，先写清背景环境、现象、复现步骤。
5. 排查中持续补充原因分析。
6. 更新两处索引：**当月** `00-README.md` 加一行（表格，含一句话 hook）+ **顶层** `00-README.md` 对应类别加一行（链接+状态）。
7. 若关联某开发任务，双向填写 `related_dev` / `related_issues`。
8. **改代码前先按第 5 节修改范式**：依根因写 UT → 确认 UT 改前必红且对准根因 → 冻结 UT → 再改业务代码至 UT 转绿（全程不回改 UT）。
9. 修复并验证后改 `已修复`，补充解决方案（含 UT 与「改前红/改后绿」证据）与修复时间，并填 `fix_commit`（修复合入的 commit id）+ 在「解决方案」写明该 commit；同步刷新两处 README 的状态。
