<!--
name: worktree并行开发约定
creator: Li Cheng
created: 2026-08-25
modified: 2026-08-25
-->

# git worktree 多特性并行开发约定

> 适用范围：`work_space` 下**同一代码仓需同时推进多个互不干扰特性**的场景。
> 本文件是 [`开发文档命名规范.md`](./开发文档命名规范.md) §6「一份计划 ↔ 一条分支」的**并行扩展**：
> 把「一条分支」升级为「一条分支 + 一个独立工作目录」，多个特性可**同时检出、各自跑测试、互不打扰**。
> 一句话：**一份开发计划 = 一条分支 = 一个 worktree 目录；主检出常驻主线，特性都在 worktree 里做。**

---

## 1. worktree 是什么 / 为什么用

`git worktree` 让**同一个仓库的多个分支同时检出到不同目录**，它们共享同一个 `.git`（对象库、历史、配置），但各自有独立的工作树与 `HEAD`。

对比「单目录 + `git switch` 切分支」的痛点：

| 单目录切分支的痛 | worktree 的解法 |
|---|---|
| 切分支要先 `stash` 未提交改动，来回易丢改动 | 每个特性一个目录，改动天然隔离，无需 stash |
| 只能同时跑一个分支的构建/测试 | 多目录并行 build / 跑测试 / 起服务，互不干扰 |
| 改 A 时来紧急 hotfix 得打断现场 | 另开一个 worktree 修 hotfix，A 的现场原封不动 |
| 想对比两个分支的运行行为很麻烦 | 两个目录并排跑，直接对比 |

> 共享 `.git` ⇒ 不重复拉全量历史，**比 `git clone` 多份省磁盘、省时间**。

---

## 2. 何时用（决策）

> **默认原则**：除非**明确声明**需要 worktree 这种并行开发模式，否则所有开发（编码、提交等）都直接在 **main 分支或当前所在的开发分支**进行——不主动新建 worktree，也不为普通任务额外拉分支。worktree 是**可选**约定，只服务"同一仓要**同时**推进多个互不干扰特性"这一个场景。

| 场景 | 用法 |
|---|---|
| 单人**串行**做一个特性 | 不必 worktree，主检出里 `git switch -c dev/...` 即可 |
| **同时**推进 ≥2 个互不干扰特性 | **用 worktree**，每特性一个目录 |
| 开发中途插入紧急 hotfix，不想打断现场 | 为 hotfix **另开一个 worktree** |
| 需并排对比两个分支的运行/测试行为 | 两个 worktree 并排跑 |

> 判据：**要不要「同时存在」两个工作现场**。要，就 worktree；不要，直接在当前分支开发。

---

## 3. 存放位置与命名（与分支一一对应）

沿用 §6「分支 ↔ 文档」的对应关系，再叠加「目录 ↔ 分支」：

```
开发文档   20260701-01-登录接口重构.md
   └─ 分支    dev/20260701-01-登录接口重构
        └─ worktree 目录   <仓名>.worktrees/20260701-01-登录接口重构/
```

### 3.1 位置约定

- **主检出**（`<仓名>/`）**常驻主线**（`main`/`master`），只用来同步主线、开分支、合入，不在其上做特性开发。
- **worktree 根目录**：主检出的**同级兄弟目录** `<仓名>.worktrees/`。放仓外（不嵌在仓内），避免污染工作树、避免 IDE/索引把 worktree 当仓内文件。

```
0-code/training/inhouse/
├── ltp_ha/                                   # 主检出，常驻 main
└── ltp_ha.worktrees/                         # 该仓所有 worktree 的根
    ├── 20260701-01-登录接口重构/              # ↔ dev/20260701-01-登录接口重构
    └── 20260702-01-推理缓存接入/              # ↔ dev/20260702-01-推理缓存接入
```

### 3.2 命名规则

| 对象 | 命名 | 示例 |
|---|---|---|
| worktree 根 | `<仓名>.worktrees/` | `ltp_ha.worktrees/` |
| 子目录名 | 分支去掉 `dev/` 前缀，即 `<日期>-<序号>-<主题>` | `20260701-01-登录接口重构/` |
| 检出分支 | `dev/<日期>-<序号>-<主题>`（变体 `fix/` `exp/`） | `dev/20260701-01-登录接口重构` |

> 目录名与分支后缀一致 ⇒ `git worktree list` 一眼对回是哪份开发文档。

---

## 4. 核心命令

以 `ltp_ha` 仓、开发文档 `20260701-01-登录接口重构.md` 为例（在**主检出目录内**执行 `add`）：

| 动作 | 命令 |
|---|---|
| 新特性（建新分支 + 目录） | `git worktree add ../ltp_ha.worktrees/20260701-01-登录接口重构 -b dev/20260701-01-登录接口重构` |
| 检出已有分支到新目录 | `git worktree add ../ltp_ha.worktrees/20260701-01-登录接口重构 dev/20260701-01-登录接口重构` |
| 查看所有 worktree | `git worktree list` |
| 合入后移除目录 | `git worktree remove ../ltp_ha.worktrees/20260701-01-登录接口重构` |
| 清理失效记录（手删过目录后） | `git worktree prune` |

进入该目录后即是普通仓，正常 `git add/commit/push`、装依赖、跑测试。合入主线仍按 §6：**在主检出**从 `dev/...` 合入 `main`。

---

## 5. 生命周期 checklist（在 §6 基础上叠加目录动作）

- [ ] **立项**：建开发文档，`**状态**：未启动`，`branch:` 留空。
- [ ] **动工**：主检出内 `git worktree add ../<仓名>.worktrees/<日期>-<序号>-<主题> -b dev/<...>` → 回填文档 `branch:` → 状态改 `开发中` → README「分支」列登记。
- [ ] **开发**：**在 worktree 目录内**改动、提交；进展记录按日期追加。
- [ ] **验证**：在该 worktree 跑通测试 / e2e；进展记录写验证结论。
- [ ] **合入**：验证通过 → 切回主检出，从 `dev/...` 合入 `main`（PR/merge）。
- [ ] **收尾**：`git worktree remove ...` 移除目录 → `git branch -d dev/...`（可选）→ 文档状态改 `已完成`，进展记录写合入 commit/PR，README 分支标「已合入」。

> worktree 目录是**临时工作现场**，合入后即可移除；分支名 + 合入状态仍按 §6.3 留在 README 可追溯，不因删目录而丢失历史线索。

---

## 6. 约束与坑（git 硬规则 + 实践）

| 事项 | 说明 |
|---|---|
| 一分支只能被一个 worktree 检出 | git 硬性限制——**天然契合**「一份计划一条分支一个目录」，不会重复检出同一分支 |
| 依赖不随工作树共享 | `node_modules/` `venv/` `build/` 等每个 worktree **各自安装/生成**（这正是并行互不干扰的来源）；可配共享缓存（pnpm store、pip cache）省空间 |
| 共享 `.git` 配置与 hooks | 所有 worktree 共用同一份 config / hooks；改全局 hook 会影响所有 worktree |
| submodule | 各 worktree 需各自 `git submodule update --init` |
| 手删目录留残留 | 直接 `rm -rf` 目录后须 `git worktree prune` 清理登记，否则 `list` 里留幽灵项 |
| 主检出别乱切 | 主检出常驻主线；特性都在 worktree 做，避免"主检出停在某特性分支"造成混乱 |
| IDE / 工具 | 每个 worktree 独立打开为一个项目窗口；索引互不干扰 |

---

## 7. 与 README / 文档追踪的衔接

- **不新增 README 列**：worktree 路径可由约定推导（`<仓名>.worktrees/<分支后缀>/`），README 仍只维护 §6.3 的「分支」列即可；路径非标准时在开发文档「进展记录」注明。
- **文档字段不变**：开发文档元信息头 `branch:` 照填分支名（不是目录路径）——分支是长期锚点，目录是临时现场。
- **权威链**：`开发文档 ↔ 分支`（§6）为准，本文件只补「分支 → 并行工作目录」这一层。

---

## 8. 速查

1. 判断：要不要**同时**存在多个工作现场？要 → worktree；不要 → 直接切分支。
2. 主检出常驻主线；新特性：`git worktree add ../<仓名>.worktrees/<日期>-<序号>-<主题> -b dev/<日期>-<序号>-<主题>`。
3. 目录名 = 分支去 `dev/` 前缀；一份文档 ↔ 一条分支 ↔ 一个目录。
4. 在该目录里开发、装依赖、跑测试；合入仍回主检出从 `dev/...` 合 `main`。
5. 合入后 `git worktree remove ...` + 可选删分支；文档改 `已完成`，README 标「已合入」。
6. 手删目录记得 `git worktree prune`。
