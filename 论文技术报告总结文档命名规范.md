<!--
name: 论文技术报告总结文档命名规范
creator: Li Cheng
created: 2026-07-28
modified: 2026-07-28
-->

# 论文 / 技术报告总结文档命名规范

> 适用范围：`/Users/a1/work/work_space` 下对**外部论文、技术报告、白皮书**做阅读总结的文档。
> 一篇总结 = 一篇论文（或一份技术报告），记录其出处、方法、结果与**对我们工作的启发**。
> 与前三类的区别：解析文档按 `序号` 排（跟随代码结构），开发/问题文档按 `日期` 排（跟随时间线），
> 论文总结按 **`发表年份`** 排 —— 检索论文时年份比"我哪天读的"更有意义。

---

## 1. 目录命名

按**主题**归拢，一个主题一个目录，与解析文档的 `-docs` 后缀呼应：

```
<主题>-papers/
```

示例：`attention-papers/`、`moe-papers/`、`rlhf-papers/`、`k8s-scheduling-papers/`。

- 主题名用英文小写，多词用连字符。
- 论文数量少时（< 5 篇）可先统一放 `papers/`，攒够再按主题拆分。
- 每个目录**必须**含 `00-README.md` 作为索引（见第 6 节）。

---

## 2. 文件命名

统一格式：

```
<发表年份>-<来源标识>-<中文主题>.md
```

| 部分 | 规则 | 示例 |
|------|------|------|
| 发表年份 | 四位 `YYYY`，取**论文发表年**（arXiv 取 v1 年份），不是阅读年 | `2017` `2024` |
| 来源标识 | 英文，无空格。优先用**方法/模型简称**，无简称时用**机构**或**一作姓氏** | `Transformer` `DeepSeek` `Vaswani` |
| 中文主题 | 简洁中文短语，点出这篇在讲什么，见名知意，不含空格 | `注意力机制取代循环结构` |
| 扩展名 | 统一 `.md` | |

示例：

```
2017-Transformer-注意力机制取代循环结构.md
2022-FlashAttention-IO感知的显存高效注意力.md
2024-DeepSeekV3-混合专家模型训练技术报告.md
2019-Google-Kubernetes大规模调度实践.md
```

约定：

- 同年同来源多篇时，在来源标识后追加 `v2` / 子主题区分，如 `2023-LLaMA2-...` / `2023-LLaMA2Chat-...`。
- 系列报告（同一模型的多版）建议保持来源标识一致，靠年份自然排序。
- 目录内按文件名排序即为**时间线顺序**，可直接看出该主题的技术演进。

---

## 3. 元信息头

每篇文件**开头第一行起**，用 HTML 注释块记录管理信息。论文类比其他三类多出**出处字段**：

```markdown
<!--
name: <中文主题，与文件名一致>
creator: Li Cheng
created: YYYY-MM-DD
modified: YYYY-MM-DD
title: <论文英文原名，照抄>
authors: <一作 et al.，或机构名>
venue: <会议/期刊/arXiv 编号，如 NeurIPS 2017 / arXiv:2205.14135>
year: <发表年份，与文件名一致>
url: <论文链接>
code: <开源实现链接，无则留空>
tags: <关键词，逗号分隔>
related_repo: <关联的解析文档目录，无则留空>
related_dev: <关联的开发文档名，无则留空>
-->
```

| 字段 | 含义 | 规则 |
|------|------|------|
| `name` | 中文主题 | 与文件名主题一致 |
| `creator` | 总结人 | 本目录默认 **Li Cheng**，他人撰写改为实际姓名 |
| `created` | 创建时间 | 首次记录日期，创建后不再改动 |
| `modified` | 最后修改时间 | 每次实质补充后改为当日 |
| `title` | 论文原名 | **照抄英文原标题**，便于精确检索与引用 |
| `authors` | 作者 | 一作 + `et al.`，或直接写机构（技术报告常无明确一作） |
| `venue` | 出处 | 会议/期刊全称+年份，预印本写 `arXiv:编号` |
| `year` | 发表年份 | 与文件名年份一致 |
| `url` | 原文链接 | 优先 arXiv abs 页 / 官方发布页 |
| `code` | 代码链接 | 官方实现优先，第三方复现需注明 |
| `tags` | 关键词 | 3~6 个，供跨目录检索，如 `注意力,显存优化,推理加速` |
| `related_repo` | 关联代码仓解析 | 若该论文对应本地已解析的仓，填 `<仓名>-docs/` |
| `related_dev` | 关联开发任务 | 若因某开发任务读此文，填开发文档名 |

> `title` / `url` 两项**必填**：没有原名和链接的总结，日后无法回溯核对，等于废稿。

---

## 4. 阅读状态（必填）

采用**最简三阶段**状态机，正文首个标题下用醒目一行标注：

```markdown
**状态**：精读中
```

| 状态 | 含义 |
|------|------|
| 速览 | 只读了摘要、图表、结论，记录核心结论备查 |
| 精读中 | 正在逐节细读，笔记持续补充 |
| 已消化 | 通读完毕，方法能复述、启发已写清 |

> 状态只进不退：`速览 → 精读中 → 已消化`。状态变更时同步更新 `modified`。
> 允许长期停在「速览」—— 不是每篇都值得精读，速览状态本身就是有效信息。

---

## 5. 正文结构（推荐，★ 为必写）

```markdown
# <年份> · <中文主题>

**状态**：精读中

> 原文：<title> — <venue> — [链接](url)

## 1. 一句话总结 ★
这篇做了什么、解决了什么、效果如何，1~3 句说清。写不出来说明还没读懂。

## 2. 背景与问题
研究的是什么问题；已有方法卡在哪里（这是判断方法价值的前提）。

## 3. 核心思想 ★
最关键的 1~3 个创新点。配关键公式（LaTeX）或结构图；
复杂结构建议用文字描述数据流向，避免只贴图不解释。

## 4. 实现细节
架构组成、算法伪码、关键超参、工程实现要点。
只记**复现时真正会卡住的细节**，不抄全文。

## 5. 实验与结果
- 数据集 / 基线 / 评测指标
- 关键结果表（只摘最能说明问题的几行）
- 消融实验说明了什么

## 6. 结论与局限
作者给出的结论；本文的局限、隐含假设、适用边界。
作者没说但你看出来的短板，标注「（笔者注）」区分开。

## 7. 个人评述与启发 ★
**本篇最有价值的部分**：
- 这个方法能不能用在我们的场景里，怎么用
- 有什么可借鉴的思路 / 明显不适用的原因
- 存疑之处、想进一步验证的点

## 8. 关联资料
- 代码仓：<链接>，本地解析见 [xx-docs](../xx-docs/00-README.md)
- 前置论文 / 后续工作：[<年份>-<来源>-<主题>](./xxx.md)
- 关联开发任务：[<开发文档名>](../xxx.md)

## 9. 术语表（可选）
本文特有的术语、缩写、符号含义。
```

原则：

- **必写三项**（一句话总结 / 核心思想 / 个人评述与启发）不可省；其余小节没内容就删掉。
- 总结 ≠ 翻译。逐段翻译原文没有价值，**提炼 + 判断**才有。
- 摘录原文用引用块并标注章节，如 `> (§3.2) ...`，与自己的话明确区分。
- 目标长度：一屏到三屏。超出说明该拆成多篇（按 `<年份>-<来源>-<子主题>` 另建）。

---

## 6. 目录索引 `00-README.md`

每个 `-papers/` 目录必须含 `00-README.md`，维护一张表：

```markdown
| 文件 | 年份 | 一句话总结 | 状态 |
|------|------|-----------|------|
| [Transformer](./2017-Transformer-注意力机制取代循环结构.md) | 2017 | 用自注意力完全替代循环结构 | 已消化 |
| [FlashAttention](./2022-FlashAttention-IO感知的显存高效注意力.md) | 2022 | 分块计算把注意力显存降到线性 | 精读中 |
```

按年份升序排列，一眼看出该主题的演进脉络与自己的阅读进度。

---

## 7. 与其他三类文档的关系

| 场景 | 处理方式 |
|------|---------|
| 论文有对应的开源仓，且已在本地解析 | 论文总结 `related_repo` 指向 `<仓名>-docs/`；解析文档 `00-README.md` 反向链回论文总结 |
| 因某开发任务去读论文 | 论文总结 `related_dev` 指向开发文档；开发文档「实现方案」节引用论文总结 |
| 论文方法落地时踩到坑 | 另建**问题记录文档**，不要写进论文总结里 —— 论文总结记录的是**外部工作**，不记录我们自己的实现问题 |

> 边界：论文总结只写「别人做了什么、我怎么看」。**我们做了什么**一律进开发文档，**我们遇到什么问题**一律进问题记录文档。

---

## 8. 新建论文总结速查

1. 选目录：`<主题>-papers/`，没有就新建并同时创建 `00-README.md`。
2. 命名：`<发表年份>-<来源标识>-<中文主题>.md`。
3. 顶部补元信息头，`title` / `url` 必填，`creator` 默认 `Li Cheng`，`created` / `modified` 填当日。
4. 正文首行标 `**状态**：速览`，先把「一句话总结」写出来。
5. 决定精读 → 改 `精读中`，逐节补充；读完写「个人评述与启发」→ 改 `已消化`。
6. 回到 `00-README.md` 追加一行索引。
7. 若关联代码仓 / 开发任务，双向填好链接。
