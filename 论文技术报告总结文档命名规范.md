<!--
name: 论文技术报告总结文档命名规范
creator: Li Cheng
created: 2026-07-28
modified: 2026-08-14
-->

# 论文 / 技术报告总结文档命名规范

> 适用范围：`work_space` 工作区下对**外部论文、技术报告、白皮书**做阅读总结的文档。
> 一篇论文 = **一个子文件夹**，内含 **原文（保真备查）+ 总结（提炼判断）+ 导航 README**。
> 与前三类的区别：解析文档按 `序号` 排（跟随代码结构），开发/问题文档按 `日期` 排（跟随时间线），
> 论文总结按 **`发表年份`** 排 —— 检索论文时年份比"我哪天读的"更有意义。

> **v2 变更（2026-08-14）**：从"一篇论文 = 一个扁平 `.md`"改为"**一篇论文 = 一个子文件夹**"。
> 动机：① 论文原文（PDF 等）必须与总结**同处归档**，保真备查、方便回溯原始信息；② 每篇自带 `README.md` 导航卡，便于查询。

---

## 1. 目录层级（两层）

按**主题**归拢，一个主题一个目录，与解析文档的 `-docs` 后缀呼应；主题目录下，**每篇论文再占一个子文件夹**：

```
<主题>-papers/                              ← 主题目录
├── 00-README.md                           ← 主题总索引（必备，见 §6）
├── <年份>-<来源>-<中文主题>/               ← 每篇一个子文件夹（见 §2）
│   ├── README.md                          ← 本篇导航卡（必备，见 §5.2）
│   ├── <原文文件名照抄>.pdf                ← 论文原文，保真备查（见 §2.2）
│   └── 总结.md                            ← 总结正文（必备，见 §5.1）
└── <年份>-<来源>-<中文主题>/
    └── ...
```

- 主题名用英文小写，多词用连字符：`attention-papers/`、`moe-papers/`、`k8s-scheduling-papers/`。
- 论文数量少时（< 5 篇）可先统一放 `papers/`，攒够再按主题拆分。
- 若沿用**日期归档**布局（如 `0-paper/<年>/<年月>/`），把每篇子文件夹直接放该月目录下，并在该月目录放 `00-README.md` 作索引 —— 两层结构不变。

---

## 2. 每篇论文的子文件夹

### 2.1 子文件夹命名

统一格式（与原扁平文件名规则一致，只是从"文件名"升级为"文件夹名"）：

```
<发表年份>-<来源标识>-<中文主题>/
```

| 部分 | 规则 | 示例 |
|------|------|------|
| 发表年份 | 四位 `YYYY`，取**论文发表年**（arXiv 取 v1 年份），不是阅读年 | `2017` `2026` |
| 来源标识 | 英文，无空格。优先用**方法/模型/框架简称**，无简称时用**机构**或**一作姓氏** | `Transformer` `Cordis` `DeepSeek` |
| 中文主题 | 简洁中文短语，点出这篇在讲什么，见名知意，不含空格 | `注意力机制取代循环结构` |

示例：

```
2017-Transformer-注意力机制取代循环结构/
2022-FlashAttention-IO感知的显存高效注意力/
2026-Cordis-时空可组合性的动态组合编程范式/
```

约定：

- 同年同来源多篇时，在来源标识后追加 `v2` / 子主题区分，如 `2023-LLaMA2-.../` 与 `2023-LLaMA2Chat-.../`。
- 目录内按子文件夹名排序即为**时间线顺序**，可直接看出该主题的技术演进。

### 2.2 原文归档（保真备查，核心）

- **必须**把论文原文（PDF；无 PDF 时存官方 HTML 快照 / Markdown 导出）放进子文件夹，**文件名照抄原始文件名**，不改名、不裁剪内容。
- 目的：保证原始信息与内容完整可查，日后核对、引用、精确检索都从这份原文出发；总结只做提炼，不替代原文。
- 原文体积大或受版权限制无法存放时，允许**只在 `README.md` 与总结里给出稳定链接**（arXiv abs 页 / 官方发布页 / 内部存储路径），并注明"原文未随档"。

---

## 3. 元信息头（写在 `总结.md`）

`总结.md` **开头第一行起**，用 HTML 注释块记录管理信息。论文类比其他三类多出**出处字段**：

```markdown
<!--
name: <中文主题，与子文件夹主题一致>
creator: Li Cheng
created: YYYY-MM-DD
modified: YYYY-MM-DD
title: <论文英文原名，照抄>
authors: <一作 et al.，或机构名>
venue: <会议/期刊/arXiv 编号，如 NeurIPS 2017 / arXiv:2205.14135>
year: <发表年份，与文件夹一致>
url: <论文链接>
code: <开源实现链接，无则留空>
tags: <关键词，逗号分隔>
related_repo: <关联的解析文档目录，无则留空>
related_dev: <关联的开发文档名，无则留空>
-->
```

| 字段 | 含义 | 规则 |
|------|------|------|
| `name` | 中文主题 | 与子文件夹主题一致 |
| `creator` | 总结人 | 默认 **Li Cheng**，他人撰写改为实际姓名 |
| `created` / `modified` | 时间 | 首次记录日期不变；每次实质补充后 `modified` 改为当日 |
| `title` | 论文原名 | **照抄英文原标题**，便于精确检索与引用 |
| `authors` | 作者 | 一作 + `et al.`，或直接写机构 |
| `venue` | 出处 | 会议/期刊全称+年份，预印本写 `arXiv:编号`，无编号写明载体 |
| `year` | 发表年份 | 与文件夹年份一致 |
| `url` | 原文链接 | 优先 arXiv abs 页 / 官方发布页 |
| `code` | 代码链接 | 官方实现优先，第三方复现需注明 |
| `tags` | 关键词 | 3~6 个，供跨目录检索 |
| `related_repo` / `related_dev` | 双向关联 | 见 §7 |

> `title` / `url` 两项**必填**：没有原名和链接的总结，日后无法回溯核对，等于废稿。

---

## 4. 阅读状态（必填，写在 `总结.md`）

采用**最简三阶段**状态机，`总结.md` 正文首个标题下用醒目一行标注：

```markdown
**状态**：精读中
```

| 状态 | 含义 |
|------|------|
| 速览 | 只读了摘要、图表、结论，记录核心结论备查 |
| 精读中 | 正在逐节细读，笔记持续补充 |
| 已消化 | 通读完毕，方法能复述、启发已写清 |

> 状态只进不退：`速览 → 精读中 → 已消化`。状态变更时同步更新 `modified`。允许长期停在「速览」。

---

## 5. 子文件夹内两份文档

### 5.1 `总结.md`（总结正文，★ 为必写小节）

```markdown
# <年份> · <中文主题>

**状态**：精读中

> 原文：<title> — <venue> — [链接](url)
> 原文 PDF 与本总结同目录，保真备查（见 README.md 文件清单）。

## 1. 一句话总结 ★
这篇做了什么、解决了什么、效果如何，1~3 句说清。写不出来说明还没读懂。

## 2. 背景与问题
研究的是什么问题；已有方法卡在哪里（这是判断方法价值的前提）。

## 3. 核心思想 ★
最关键的 1~3 个创新点。配关键公式（LaTeX）或结构图；复杂结构用文字描述数据流向，避免只贴图不解释。

## 4. 实现细节
架构组成、算法伪码、关键超参、工程实现要点。只记**复现时真正会卡住的细节**，不抄全文。

## 5. 实验与结果
数据集 / 基线 / 指标；关键结果表（只摘最能说明问题的几行）；消融说明了什么。

## 6. 结论与局限
作者结论；本文局限、隐含假设、适用边界。作者没说但你看出来的短板标注「（笔者注）」。

## 7. 个人评述与启发 ★
**本篇最有价值的部分**：能否用在我们场景、怎么用；可借鉴思路 / 不适用原因；存疑与待验证点。

## 8. 关联资料
代码仓、前置/后续论文、关联开发任务的链接。

## 9. 术语表（可选）
本文特有的术语、缩写、符号含义。
```

原则：

- **必写三项**（一句话总结 / 核心思想 / 个人评述与启发）不可省；其余小节没内容就删。
- 总结 ≠ 翻译。逐段翻译原文没有价值，**提炼 + 判断**才有。
- 摘录原文用引用块并标注章节，如 `> (§3.2) ...`，与自己的话明确区分。
- 目标长度：一屏到三屏。超出说明该拆成多篇（按 `<年份>-<来源>-<子主题>/` 另建子文件夹）。

### 5.2 `README.md`（本篇导航卡，必备）

一张速查卡 + 文件清单，让人不打开总结就能判断"这篇讲什么、原文在哪、读到哪了"：

```markdown
# <年份> · <中文主题>

## 速查卡
| 项 | 内容 |
|----|------|
| 英文原名 | <title> |
| 作者 | <authors> |
| 年份 | <year> |
| 出处 | <venue> |
| 代码 | <code> |
| 状态 | <速览 / 精读中 / 已消化> |
| 一句话 | <一句话总结> |

## 文件清单
| 文件 | 用途 |
|------|------|
| [`总结.md`](./总结.md) | 总结正文（★ 必读） |
| `<原文文件名>.pdf` | **论文原文**，保真备查 |
| `README.md` | 本导航卡 |
```

---

## 6. 主题总索引 `00-README.md`

每个 `-papers/` 主题目录（或日期归档的月目录）必须含 `00-README.md`，维护一张表，**指向每篇的子文件夹 / 总结**：

```markdown
| 论文（文件夹） | 年份 | 来源 | 一句话总结 | 状态 | 总结 |
|---------------|------|------|-----------|------|------|
| [Transformer](./2017-Transformer-注意力机制取代循环结构/README.md) | 2017 | Transformer | 用自注意力完全替代循环结构 | 已消化 | [总结.md](./2017-Transformer-注意力机制取代循环结构/总结.md) |
| [FlashAttention](./2022-FlashAttention-IO感知的显存高效注意力/README.md) | 2022 | FlashAttention | 分块计算把注意力显存降到线性 | 精读中 | [总结.md](./2022-FlashAttention-IO感知的显存高效注意力/总结.md) |
```

按年份升序排列，一眼看出该主题的演进脉络与自己的阅读进度。

---

## 7. 与其他三类文档的关系

| 场景 | 处理方式 |
|------|---------|
| 论文有对应开源仓，且已在本地解析 | 总结 `related_repo` 指向 `<仓名>-docs/`；解析文档 `00-README.md` 反向链回 |
| 因某开发任务去读论文 | 总结 `related_dev` 指向开发文档；开发文档「实现方案」节引用总结 |
| 论文方法落地时踩到坑 | 另建**问题记录文档**，不写进总结 —— 总结记录**外部工作**，不记录我们自己的实现问题 |

> 边界：总结只写「别人做了什么、我怎么看」。**我们做了什么**进开发文档，**我们遇到什么问题**进问题记录文档。

---

## 8. 新建论文总结速查

1. 选主题目录：`<主题>-papers/`（或日期归档的月目录），没有就新建并同时创建 `00-README.md`。
2. **建子文件夹**：`<发表年份>-<来源标识>-<中文主题>/`。
3. **放原文**：把论文 PDF（照抄原名）复制进子文件夹，保真备查（§2.2）。
4. 写 `总结.md`：补元信息头（`title` / `url` 必填，`creator` 默认 `Li Cheng`），首行标 `**状态**：速览`，先写「一句话总结」。
5. 写 `README.md`：填速查卡 + 文件清单。
6. 精读 → 改 `精读中` 逐节补充；读完写「个人评述与启发」→ 改 `已消化`。
7. 回主题 `00-README.md` 追加一行索引（指向子文件夹 / 总结）。
8. 若关联代码仓 / 开发任务，双向填好链接。
