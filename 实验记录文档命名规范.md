<!--
name: 实验记录文档命名规范
creator: Li Cheng
created: 2026-08-25
modified: 2026-08-25
-->

# 实验记录文档命名规范

> 适用范围：`work_space` 工作区下记录**一次实验 / 演练 / 基准测试**的文档。
> 一篇实验记录 = 一次可复现的实验，记录其**假设、配置、方法、证据、结论**全过程。
> 命名格式**参照开发文档**，以日期开头。

---

## 0. 什么算「实验记录」（与其它四类的边界）

| 场景 | 归到 |
|---|---|
| 故障演练、HA/容灾验收、注障验收 | **实验记录** ✅ |
| 性能基准 / 带宽压测 / 吞吐测试 | **实验记录** ✅ |
| 消融实验、超参对比、精度对拍 | **实验记录** ✅ |
| 记录代码仓结构 | 解析文档 |
| 做一项功能开发 | 开发文档 |
| 排查一个 bug | 问题记录 |
| 总结外部论文 | 论文总结 |

判据：**为回答一个具体问题而主动构造一次带对照/可观测的运行，并用实测数据下结论**——就是实验记录。它不是"改代码"（开发文档），也不是"修 bug"（问题记录），而是"跑一次、看现象、下结论"。

> 实验中若**暴露出 bug** → 另起问题记录，本文 `related_issues` 关联；若由此**发起改造** → 另起开发文档，本文 `related_dev` 关联。实验记录只对"这次实验看到了什么、说明什么"负责。

---

## 1. 目录结构与文件命名

### 1.1 目录

实验记录归入**其所属代码仓**的 docs 下，统一文件夹名 `experiment_results/`（英文、复数）：

```
<repo>/docs/experiment_results/
├── 00-README.md                                   ← 索引（按时间列全部实验，表格）
├── 20260825-01-cordon-kill-worker-HA演练结果.md
└── 20260826-01-PCIe-H2D带宽基准.md
```

- 与仓强绑定的实验放该仓 `docs/experiment_results/`；跨仓或无归属的独立实验，可放工作区级 `0-experiments/experiment_results/`。
- 文件夹名**固定英文** `experiment_results`，不用中文，便于脚本/路径引用。

### 1.2 文件命名（与开发文档一致）

```
<日期>-<当日序号>-<实验主题>.md
```

| 部分 | 规则 | 示例 |
|------|------|------|
| 日期 | 八位 `YYYYMMDD`，为实验**执行当天** | `20260825` |
| 当日序号 | 两位数字，从 `01` 起，同一天内递增 | `01` `02` |
| 实验主题 | 简洁中文短语，见名知意，含"测什么/什么场景"，不含空格 | `cordon-kill-worker-HA演练结果` `PCIe-H2D带宽基准` |
| 扩展名 | 统一 `.md` | |

---

## 2. 元信息头

每篇开头第一行起，用 HTML 注释块记录：

```markdown
<!--
name: <实验主题，与文件名一致>
creator: Li Cheng
created: YYYY-MM-DD
modified: YYYY-MM-DD
category: <受控类别集之一，见 2.1>
related_dev: <因本实验发起/验证的开发文档，无则留空或写 ->
related_issues: <本实验暴露的问题记录文档，无则留空或写 ->
related_repo: <被测代码仓的解析文档目录，无则留空或写 ->
-->
```

| 字段 | 含义 | 规则 |
|------|------|------|
| `name` | 实验主题 | 与文件名一致 |
| `creator` | 执行人 | 默认 **Li Cheng** |
| `created` | 执行日期 | 首次记录日，创建后不改 |
| `modified` | 最后修改 | 每次更新改当日 |
| `category` | 实验类别 | 从受控类别集取值（见 2.1） |
| `related_dev` | 关联开发 | 因本实验发起或用以验证的开发文档 |
| `related_issues` | 关联问题 | 本实验暴露的 bug 的问题记录 |
| `related_repo` | 被测仓 | 指向 `<仓名>-docs/`，标明测的是哪套代码 |

### 2.1 受控类别集（`category` 取值）

| 类别 | 覆盖 |
|---|---|
| `HA·容灾/故障演练` | cordon/kill、坏节点迁移、注障验收、断点续训 |
| `性能·带宽/吞吐` | PCIe/NVLink 带宽、通信、算力吞吐、扩展性 |
| `训练·收敛/精度` | loss 曲线、消融、超参对比、精度对拍 |
| `调度·编排` | Volcano/gang/抢占/排队行为 |
| `其他` | 兜底 |

---

## 3. 实验状态（必填）

正文首个标题下醒目一行标注，三阶段只进不退：

```markdown
**状态**：已完成
```

| 状态 | 含义 |
|------|------|
| 设计中 | 已定假设与方案，尚未执行 |
| 进行中 | 正在跑 / 分阶段进行，证据陆续补充 |
| 已完成 | 全部执行完毕，结论已定 |

---

## 4. 正文结构（推荐）——实验记录三原则

实验记录区别于其它文档的核心是**三原则**，正文结构即服务于此：

> **① 可复现**：配置、版本、命令钉死，别人照着能再跑一遍。
> **② 有证据**：每个结论都贴实测日志/指标，带时间戳，不凭印象。
> **③ 假设-结论闭环**：开头写清"预期看到什么"，结尾明确"实际是否吻合"。

```markdown
# <实验主题>

**状态**：已完成

## 0. 结论速览
一张表给出关键结论 + 一段"核心洞见"。让读者 30 秒抓住结果。

## 1. 目的与假设（对应原则③上半）
- 要回答什么问题 / 验证什么。
- **预期结果**（做之前就写下，避免"结果出来再圆"）。

## 2. 环境与配置（对应原则①）
- 集群 / 节点 / 硬件；被测仓 commit、依赖版本、镜像。
- 关键启动配置（贴 YAML/命令片段，钉死可复现的最小集）。

## 3. 方法与步骤（对应原则①）
- 实际怎么操作的，分阶段/编号步骤，贴**可直接复制的命令**。
- 对照/变量设计（若有）。

## 4. 观测与证据（对应原则②，本文的主体）
- 按阶段/指标组织，每个现象**紧跟实测日志片段或指标数字 + 时间戳**。
- 原则：先摆证据，再下判断；结论句旁必有它依据的那几行日志。

## 5. 结论（对应原则③下半）
- 逐条回答第 1 节的问题：预期 vs 实际，吻合 / 偏差。
- 核心洞见、边界与局限。

## 6. 遗留与待办
- 暴露的缺口、未覆盖的场景、下一步实验；关联 `related_issues`/`related_dev`。

## 7. 复现备忘（对应原则①）
- 一段可照抄的命令序列 + 观测点路径，供再次复现。

## 8. 清理
- 实验对环境造成的改动（cordon、临时任务、脏数据）如何复位；定稿前**核实真实现状**再写结论。
```

空节可省，但 **0 结论速览 / 1 假设 / 4 证据 / 5 结论** 四节不可省——它们承载三原则。

---

## 5. 证据规范（实验记录的生命线）

结论无实测证据 = 无效实验记录。硬性要求：

1. **每个关键结论旁贴证据**：日志片段、指标数字、截图路径，三选一以上。
2. **带时间戳与来源**：日志标明时刻与文件（如 `supervisor_node1.log`）；跨时区实验**统一时区并注明**换算（如"集群 UTC，命令 UTC+8"）。
3. **保留原始数据**：关键 raw 日志/csv 随文档同目录归档或给稳定路径，别只留结论。演练中变更了节点状态的，**定稿前重新核实现状**（见 §4 第 8 节），防止拿中间态快照当终态。
4. **摘录即标注**：贴的是原始日志用代码块；自己的解读用正文，二者不混。

---

## 6. 索引与互链

### 6.1 目录内 `00-README.md`（按时间列表）

`experiment_results/00-README.md` 用表格按时间倒序列全部实验：

| 列 | 内容 |
|---|---|
| 日期 / 文档 / 场景 / 终态或关键结论 / 状态 | 一句话 hook 的单一来源在此 |

### 6.2 与其它文档互链

- 实验暴露 bug → 建问题记录，双向填 `related_issues` ↔ 问题文档 `related_dev`（或新增 `related_experiment`）。
- 实验验证某开发 → 开发文档进展记录里引本实验；本文 `related_dev` 回指。
- 测的是某仓 → `related_repo` 指向该仓解析文档。

---

## 7. 新建实验记录速查

1. 定位目录：被测仓 `docs/experiment_results/`（无则建）。
2. 命名：`<执行日期>-<当日序号>-<实验主题>.md`。
3. 补元信息头：`creator` 默认 Li Cheng，`category` 从受控集取，填 `related_repo`。
4. **动手前**先写「0 结论速览」占位 +「1 目的与假设」的预期结果，再执行。
5. 执行中按阶段在「4 观测与证据」贴实测日志（带时间戳/来源）。
6. 完成后回填「5 结论」（预期 vs 实际）、「6 遗留」，状态改 `已完成`。
7. **定稿前核实环境真实现状**，写「8 清理」；关键 raw 数据归档。
8. 更新 `00-README.md` 加一行（含一句话 hook）；有关联则双向填 `related_dev`/`related_issues`。
