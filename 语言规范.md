<!--
name: 语言规范
creator: Li Cheng
created: 2026-09-21
modified: 2026-09-21
-->

# 语言规范（跨全部五类文档，也适用于对话与 commit message）

**状态**：生效中。

> 定位：这条规范管的不是文件叫什么名字，而是**字怎么写**。五类文档（解析 / 开发 / 问题记录 / 论文总结 / 实验记录）全部适用；对话回复、commit message、代码注释同样适用。
> 来源：2026-09-21 用户明确要求——「所有输出和讲解都使用通俗易懂的语言，严禁使用自创的专有名词，通用的英文专有名词就保持英文，不要随便翻译成中文」，并指出**不说人话就是在增加沟通成本**。

## 1. 四条规矩

| # | 规矩 | 说明 |
|---|---|---|
| 1 | **用通俗易懂的语言** | 让不熟悉这块的人也能看懂。判据：一个没有上下文的人读完，能说出「这东西是干什么的」。 |
| 2 | **严禁自创专有名词** | 不发明缩写、不造词、不把一个普通概念包装成看起来很专业的新名字。 |
| 3 | **通用英文术语保持英文** | 不要随便译成中文。 |
| 4 | **先说干什么，再说细节** | 解释任何东西，第一句话交代它的作用，之后才展开机制、参数、边界。 |

## 2. 哪些词保持英文

这些是行业通用叫法，写英文别人一眼就懂，译成中文反而要猜：

`wheel`、`digest`、`hash`、`sha256`、`dry-run`、`base image`、`registry`、`tag`、
`commit`、`branch`、`merge`、`rebase`、`verify`、`build`、`context`、`daemon`、
`checkpoint`、`rank`、`timeout`、`fallback`、`import`、`pod`、`job`、`token`

反面例子（**不要这样写**）：

| 不要 | 要 |
|---|---|
| 轮子 / 轮子包 | `wheel` |
| 摘要（指镜像内容标识时） | `digest` |
| 空跑 / 干跑 | `dry-run` |
| 基底镜像 / 底座镜像 | `base image` |
| 提交物 / 变更集 | `commit` |
| 兜底回退 | `fallback` |

例外：**有公认中文说法的就用中文**，比如「镜像」（image）、「容器」（container）、「分支」（branch 在中文语境已通行）、「超时」。判断标准是哪个更好懂，不是硬性一边倒。

## 3. 什么算「自创专有名词」

只要满足任一条，就是自创，必须换掉或当场解释：

1. 这个词只在我们自己的对话或文档里出现过，搜不到公认定义；
2. 它是我为了省字而造的缩写；
3. 它把一个动作包装成了名词，读者必须回头问「这是什么」。

已经发生过的真实例子：

- ❌ 「牙齿」——指「临时改坏生产代码，确认测试确实会变红，再还原」。用户直接问「牙齿是个什么东西」。
  ✅ 改成：「变异校验（临时改坏代码，确认测试确实会失败，再还原）」。

处理办法：第一次出现时用白话写清楚，**要么彻底不用这个词**，要么写成「白话（附一句解释）」。黑话可以留在口头对话里，不能进文档。

## 4. 落笔前的三条自检

1. 这段里有没有只有我懂的词？有 → 换白话，或当场加一句解释。
2. 有没有把 `wheel`、`digest` 这类通用英文术语硬译成中文？有 → 改回英文。
3. 开头有没有先说清「这东西是干什么的」？没有 → 重写开头。

## 5. 与其它规范的关系

- [`开发文档命名规范.md`](./开发文档命名规范.md) §6.6：有序步骤拆条、不写一坨——那条管**结构**，这条管**用词**，两条一起用。
- [`README.md`](./README.md)：规范库总入口。
