<!--
name: 开发文档命名规范
creator: Li Cheng
created: 2026-07-01
modified: 2026-09-13
-->

# 开发文档命名规范

> 适用范围：`work_space` 工作区下记录**开发任务**的文档。
> 一篇开发文档 = 一项开发任务，记录其目标、方案与全过程进展。
> 命名与解析文档区别开：解析文档按序号编号，开发文档一律**以日期开头**。

---

## 1. 文件命名

统一格式：

```
<日期>-<当日序号>-<任务主题>.md
```

| 部分 | 规则 | 示例 |
|------|------|------|
| 日期 | 八位 `YYYYMMDD`，为任务**创建当天** | `20260701` |
| 当日序号 | 两位数字，从 `01` 起，同一天内递增 | `01` `02` `03` |
| 分隔符 | 各部分之间用连字符 `-` | |
| 任务主题 | 简洁中文短语，**语义清晰、见名知意**，不含空格；避免 `开发文档1` 这类无意义命名 | `登录接口重构` `数据管线优化` |
| 扩展名 | 统一 `.md` | |

示例：

```
20260701-01-登录接口重构.md
20260701-02-数据管线优化.md
20260702-01-推理缓存接入.md
```

> 同一天的多篇开发文档用 `-01` `-02` 递增区分；跨天则日期变化，序号重新从 `01` 起。

---

## 2. 元信息头

每篇文件**开头第一行起**，用 HTML 注释块记录管理信息（不影响 Markdown 渲染）：

```markdown
<!--
name: <任务主题，与文件名一致>
creator: Li Cheng
created: YYYY-MM-DD
modified: YYYY-MM-DD
branch: <对应开发分支名，未建则留空，见第 6 节>
related_issues: <关联的问题记录文档，无则留空或写 -->
```

| 字段 | 含义 | 规则 |
|------|------|------|
| `name` | 任务主题 | 与文件名主题一致 |
| `creator` | 创建人 | 本目录默认 **Li Cheng**，他人创建改为实际姓名 |
| `created` | 创建时间 | 首次创建日期，创建后不再改动 |
| `modified` | 最后修改时间 | 每次实质性更新后改为当日 |
| `branch` | 对应开发分支 | 一份开发计划对应一条独立分支，见第 6 节；纯文档/未动工可留空 |
| `related_issues` | 关联问题 | 填对应的问题记录文档名（可多个），见第 5 节 |

---

## 3. 开发状态（必填）

采用**最简三阶段**状态机，正文首个标题下用醒目一行标注：

```markdown
**状态**：开发中
```

| 状态 | 含义 |
|------|------|
| 未启动 | 已建文档、明确目标，但尚未动工 |
| 开发中 | 正在实现，进展记录持续追加 |
| 已完成 | 任务完成并验证通过 |

> 状态只进不退：`未启动 → 开发中 → 已完成`。状态变更时同步更新 `modified`。

---

## 4. 正文结构（推荐，保持精简）

按以下小节组织，**没有内容的小节可省略**，避免冗长：

```markdown
# <日期>-<序号> · <任务主题>

**状态**：开发中

## 1. 目标与背景
做什么、为什么做（1~3 句说清）。

## 2. 影响范围
涉及的代码仓 / 模块 / 关键文件。

## 3. 实现方案
关键设计与实现要点（列要点即可，不写长篇）。

## 4. 进展记录
按日期倒序或正序追加，天然形成时间线：
- 2026-07-01：完成初版接口，待联调。
- 2026-07-02：联调通过，补充单测。

## 5. 关联问题
开发中发现的问题，链接到对应问题记录文档（见第 5 节）。

## 6. 验证与结果
如何验证（测试、运行结果），完成后填写。
```

原则：**能一屏看懂**。写清目标、方案、进展即可，不追求面面俱到。

---

## 5. 与问题记录文档的对应

开发过程中发现的问题，**另建问题记录文档**（见 `问题记录文档命名规范.md`），并双向关联：

- 开发文档：元信息头 `related_issues` + 第 5 节列出关联问题文档名。
- 问题文档：元信息头 `related_dev` 指回本开发文档名。

示例对应：

```
开发文档  20260701-01-登录接口重构.md
              └─ related_issues: 20260701-02-登录超时问题.md
问题文档  20260701-02-登录超时问题.md
              └─ related_dev: 20260701-01-登录接口重构.md
```

---

## 6. 开发分支与文档追踪约定（重要）

**一份开发计划 = 一条独立开发分支**。不在主线（`main`/`master`）上直接改；每份开发文档对应一条自己的分支，
**效果验证通过后才合入主线**，开发文档全程**跟踪**该分支的状态。目的：改动可隔离、可回滚、可追溯，且从文档一眼查到「这份计划在哪条分支、验没验、合没合」。

### 6.1 分支命名

与开发文档一一对应，便于双向追溯：

```
dev/<日期>-<当日序号>-<主题精简>
```

示例：开发文档 `20260701-01-登录接口重构.md` → 分支 `dev/20260701-01-登录接口重构`（主题过长可精简，但须能对回文档）。

> 修 bug 类可用 `fix/<主题>`，实验探索类可用 `exp/<主题>`；核心要求是**名字能对回那份开发文档**。

### 6.2 生命周期（分支状态 ↔ 文档状态）

| 阶段 | 分支动作 | 开发文档 `**状态**` | `branch` 字段 / 进展记录 |
|------|---------|---------------------|--------------------------|
| 立项 | 尚未建分支 | `未启动` | `branch` 留空 |
| 开发 | 从主线拉出 `dev/...` 分支 | `开发中` | 填 `branch:`；进展记录追加「已建分支」 |
| 验证 | 分支上跑通测试/实验 e2e | `开发中`（待验证） | 进展记录写验证结论 |
| 合入 | 验证通过 → 合入主线 → 分支归档/删除 | `已完成` | 进展记录写「已合入主线，commit/PR: xxx」 |

**原则**：文档状态与分支进度同步推进；`已完成` 意味着**已验证并已合入主线**（不是"分支上写完了"）。

> **同一仓要并行推进多个特性**时，把「一条分支」升级为「一条分支 + 一个独立工作目录」，用 git worktree 让多特性同时检出、各自跑测试、互不打扰。见 [`worktree并行开发约定.md`](./worktree并行开发约定.md)。

### 6.3 README 记录所有开发计划及其分支（可查性）

每个项目的 README「开发文档一览」表**增设「分支」列**（或「分支 / 合入」列），记录每份开发计划对应的分支名与合入状态，
使人**从 README 一处即可查到全部开发计划 ↔ 分支 ↔ 是否合入**。示例列头：

```
| 文档 | 状态 | 分支 | 一句话 |
```

> 分支删除后（已合入主线）保留分支名 + 标注「已合入」即可，便于日后按分支名追溯历史改动。

### 6.4 commit 是文档与代码的锚点（问题 ↔ 修复 commit，类比 issue ↔ PR）

分支上的每次实质改动都落成 commit；文档通过 **commit id** 对回具体代码变更：

- **开发文档**：进展记录里的关键节点标注 commit / PR（如「2026-07-02：接口联调通过，commit a1b2c3d」），合入主线时记合入 commit/PR。
- **问题 ↔ 修复 commit**：问题修复后，问题记录文档填 `fix_commit`（修复合入的 commit id）——**像 issue 被某个 PR 关闭**，让「这个问题哪次改动修的」可一键回溯。详见 `问题记录文档命名规范.md` §2/§5。
- 原则：**能从文档跳到 commit、从 commit 追回文档**。分支名回答"在哪条线开发"，commit id 回答"具体哪次改动"。

### 6.5 阶段验收表与逐项提交纪律

需要分阶段实施的开发计划必须包含“阶段验收表”。验收表按可独立交付的最小验收项逐行记录：编号、阶段/事项、完成条件、UT Red/Green 命令与结果、原始证据路径、状态、验收日期、实现 commit、文档回填 commit。

每一项严格执行：设计 UT → 运行 Red → 最小实现 → 运行 Green → 验收 → 提交该项代码/测试 commit → 将真实 commit、证据和结论回填验收表。前一项未通过不得进入依赖它的下一项；checkpoint、测试通过但未提交或未回填，均不得标记完成。失败命令、原因和遗留条件必须保留。

阶段验收表建议使用以下列：

`编号 | 阶段/验收项 | 完成验收条件 | Red/Green 命令与结果 | 证据路径 | 状态 | 验收日期 | 该项 commit | 回填 commit`

回填文档可单独提交 `docs(plan)` commit，不得 amend 已记录的实现 commit；阶段状态只有在验收和提交信息均已回填后才能更新。

### 6.6 回填一律分条，不写一坨（格式铁律）

回填**必须逐条拆分**，禁止把多条事实压成一大段话（同 `structured-writing`：有序步骤不写一坨）。适用于**所有回填出口**：进展记录（§4/§7）、验收表单元格、逐项回传模板。

1. **进展记录（正文）**：一行日期标题 + 若干缩进子 bullet，每条独立成行（如 落点 / 证据 / 已捕获清单 / 延后项 / 已知红点 / 澄清 各一条）。不把六件事写进一个句子。
2. **验收表单元格**：Markdown 表格内真换行会破表，用 `<br>` + 编号（①②③）分条；「完成验收条件」「Red/Green 命令与结果」等长单元格逐条列，一条一 `<br>`。
3. **逐项回传模板**：按模板字段（编号/阶段/日期/commit/环境/命令/结果/证据/回滚点/遗留）逐行填，Red 与 Green 各自成行，不合并。
4. **判据**：任一回填单元格/段落出现 3 个及以上并列事实用顿号/逗号串联即为「一坨」，须拆条。

---

## 7. 新建开发文档速查

1. 命名：`<今天日期>-<当日序号>-<主题>.md`（序号当日从 `01` 递增）。
2. 顶部补元信息头，`creator` 默认 `Li Cheng`，`created`/`modified` 填当日，`branch` 先留空。
3. 正文首行标 `**状态**：未启动`，写明目标与背景。
4. 动工时：从主线拉 `dev/<日期>-<序号>-<主题>` 分支 → 回填 `branch:` → 状态改 `开发中` → README 一览表补「分支」列。
5. 持续追加进展记录；遇到问题→建问题记录文档并双向关联。
6. **验证通过 → 合入主线** → 状态改 `已完成`，进展记录写合入 commit/PR，README 标注分支「已合入」。
