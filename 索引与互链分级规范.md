<!--
name: 索引与互链分级规范
creator: Li Cheng
created: 2026-08-25
modified: 2026-08-25
-->

# 索引与互链分级规范

> 适用范围：`/Users/a1/work/work_space` 下所有索引（README）与文档间的互链关系。
> 目的：让**总目录不随文件增多而臃肿**——总导航只登记入口、逐级下钻，叶子文档不直接挂到总导航上。
> 与前四类命名规范（解析 / 开发 / 问题 / 论文）正交：那四类管**单篇怎么命名**，本规范管**多篇之间怎么连**。

---

## 1. 三级结构（一句话）

导航分三级，**只能逐级下钻，不许跨级链叶子**：

| 级 | 是谁 | 典型文件 | 只做一件事 |
|---|---|---|---|
| **L0 总导航** | 工作总入口 | `0-docs/README.md` | 登记**工作线 / 各部分入口**，指向 L1 |
| **L1 部分索引** | 每个部分的门面 | `<part>/README.md`、`<仓名>-docs/00-README.md`、`problems/00-README.md`、论文月 `00-README.md` | 索引本部分的**叶子**清单 |
| **L2 叶子文档** | 一篇具体文档 | 一篇开发 / 问题 / 论文 / 解析文档 | 内容本体；向上指 L1、同级靠元字段互链 |

> 记忆法：**L0 指文件夹，L1 指文件，L2 指同侪**。

---

## 2. 三条铁律

| # | 规则 | 反例（禁止） |
|---|---|---|
| 1 | **L0 只链到「文件夹」或「部分 README / 代码仓文件夹」**，不链任何叶子 `.md` | ❌ 总导航里直接列某一篇开发文档 / 某一个问题 |
| 2 | **新增叶子只改 L1**，L0 零改动 | ❌ 每写一篇文档就回去改总导航 |
| 3 | **每个「部分」必须有唯一一个 L1 索引**（README / 00-README），叶子的清单只在这里维护 | ❌ 一个部分没有 README，叶子散落、只能靠总导航兜底 |

L0 **唯一**需要手动保鲜的信息是「工作线状态」；其余靠下钻，改动落在 L1。

---

## 3. 何时才动 L0（总导航）

只有「入口级」变化才回改总导航，日常写文档不动它：

| 场景 | 动 L0 吗 | 动作 |
|---|---|---|
| 某部分**新增一篇**开发 / 问题 / 论文 / 解析文档 | ❌ 否 | 只改该部分 L1 索引 |
| **新开一条工作线** | ✅ 是 | L0 加一行，主入口指向该线的 README 或最新计划 |
| **新增一个代码仓** | ✅ 是 | L0「代码仓 × 解析文档」加一行，指向仓文件夹 |
| 工作线**状态变化** | ✅ 是 | L0 改「状态」一格 |

---

## 4. 反向互链（保证分级不断链）

L2 叶子不是孤岛，但**绝不向上跳到 L0**，只做两类连接：

| 方向 | 怎么连 | 依据 |
|---|---|---|
| **向上（子→父）** | 叶子在正文/引用块指回**本部分的 L1 README** | 让人从任意一篇能回到该部分总览 |
| **同级（叶子↔叶子）** | 靠**元信息头字段**互链，不靠散链 | `related_dev` / `related_issues` / `related_repo` / `fix_commit`（见各类规范） |

> 同级互链一律走元字段（结构化、可检索），不在正文里随手贴一堆兄弟文档链接——那会退化成「小型总导航」，重蹈臃肿。

---

## 5. 两种索引文件名（何时用哪个）

L1 索引统一叫 README，按所在部分选前缀：

| 文件名 | 用在 | 原因 |
|---|---|---|
| `README.md` | 工作线目录、规范文件夹、代码仓根 | GitHub/编辑器默认识别，进目录即见 |
| `00-README.md` | **带两位序号编号**的文档集：`<仓名>-docs/`、`problems/<年>/<年-月>/`、论文主题/月目录 | `00-` 前缀排在序号叶子最前，与叶子同一排序体系 |

---

## 6. 本规范的活样板

`0-docs/README.md` 就是严格按本规范写的 L0：

- §1 工作线 → 指向各线自维护 README（如 `LTP_plan/README.md`），不列具体计划篇目。
- §2 代码仓 → 一行一个仓**文件夹** + 其 `-docs/` 文件夹，不列解析文档具体篇。
- §3/§4 问题/论文 → 只给 `problems/00-README.md`、`paperhub-lc/` 入口，明言「本页不枚举具体篇目」。

而 `docs-naming-convention/README.md` 是一个 L1：它索引本文件夹内 5 类规范 + skill + examples，供 L0 的 §5 以「文件夹级」一条链接下钻。**总导航 → 本文件夹 README → 各规范**，即三级下钻的最短实例。

---

## 7. 新建 / 维护速查

1. 写一篇新文档（L2）→ 只更新**它所在部分的 L1 README**，L0 不动。
2. L2 正文补一条**指回本部分 L1 README** 的链接；与兄弟文档的关联走**元字段**，不散链。
3. 新开一个部分（工作线 / 代码仓 / 问题月 / 论文月）→ 先给它建一个 **L1 索引**（`README.md` 或 `00-README.md`），再回 L0 加**一行入口**。
4. 需要在 L0 加东西时，先自问：**这是「入口级」变化吗？**（新工作线 / 新代码仓 / 状态变化）不是，就说明该改的是 L1。
